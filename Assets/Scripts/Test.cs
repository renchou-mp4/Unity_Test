using UnityEngine;

public class Test : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        //LogManager.Log("¿ªÊ¼");
        //string[] allFilesPath = Directory.GetFiles(Application.dataPath + "/Bundle", "*", SearchOption.AllDirectories);

        //foreach (string filePath in allFilesPath)
        //{
        //    LogManager.Log(filePath.ReplacePathBackslash());
        //}
    }

    // Update is called once per frame
    void Update()
    {

    }
}
